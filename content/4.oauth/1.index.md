---
title: OAuth & Sign in with Sporty
description: Let people connect your application to their Sporty account, with their permission and nothing more.
navigation:
  icon: sporty-brackets-curly
seo:
  title: Sporty OAuth 2.1 and OpenID Connect
  description: Authorization code with PKCE, refresh tokens, client credentials, and OpenID Connect on api.sporty.plus.
---

Sporty is an OAuth 2.1 and OpenID Connect provider. Your application can ask a member for
permission, receive a scoped access token, and use it to read the things they agreed to —
without ever seeing their password, and with the member able to take that access back at any
time.

Everything here is standard. Any OAuth or OIDC client library will work; you should not need
to write anything Sporty-specific.

::callout{icon="i-heroicons-information-circle"}
**Not to be confused with the older external API.** [`/api/external/v1`](/api) is a separate,
older surface authenticated with a club id and a shared key, and it is unrelated to
everything on this page. New integrations should use OAuth and the partner API described here.
::

## Which flow you want

| You are building | Use |
| --- | --- |
| A web, mobile or desktop app that acts for the person using it | **Authorization code + PKCE** |
| "Sign in with Sporty" — you just need to know who someone is | **Authorization code + PKCE**, with the `openid` scope |
| A server-to-server integration acting for a club, with no person present | **Client credentials** |
| An AI agent or MCP client | **Authorization code + PKCE**, usually with [dynamic registration](/oauth/mcp-and-ai-clients) |

There is no password grant and no implicit grant. OAuth 2.1 removes both.

## Endpoints

Everything lives on `https://api.sporty.plus`.

| Purpose | Endpoint |
| --- | --- |
| Authorization | `GET /oauth2/authorize` |
| Token | `POST /oauth2/token` |
| Revocation ([RFC 7009](https://www.rfc-editor.org/rfc/rfc7009)) | `POST /oauth2/token/revoke` |
| Dynamic registration ([RFC 7591](https://www.rfc-editor.org/rfc/rfc7591)) | `POST /oauth2/register` |
| UserInfo | `GET /oauth2/userinfo` |
| JWKS | `GET /oauth2/jwks` |

Rather than hard-coding those, fetch one of the discovery documents:

```bash
curl https://api.sporty.plus/.well-known/openid-configuration
curl https://api.sporty.plus/.well-known/oauth-authorization-server
curl https://api.sporty.plus/.well-known/oauth-protected-resource
```

## Where tokens work

An access token is spent on the **partner API**, `https://api.sporty.plus/api/partner/v1`.

It will not work anywhere else. In particular `/api/v3`, the API our own apps use, refuses
OAuth tokens outright and answers `403` with a message saying so. That surface is authorised
for people using our own interface, not for applications acting on their behalf, and letting
a token in there would grant far more than any consent screen described.

## Next

- [Registering an application](/oauth/clients)
- [The authorization code flow, step by step](/oauth/authorization-code)
- [Scopes](/oauth/scopes)
- [Sign in with Sporty](/oauth/sign-in-with-sporty)
- [The partner API](/oauth/partner-api)
- [MCP and AI clients](/oauth/mcp-and-ai-clients)
